[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.args]
NODE_ENV = "production"

[build.phases.setup]
cmds = [
    ["npm", "ci", "--production"]
]

[build.phases.install]
cmds = []

[build.phases.build]
cmds = []